---
import BlogArchiveList from "./BlogArchiveList.astro";
import { getLatestPosts } from "../lib/get-latest-posts";

const { posts } = await getLatestPosts({ includeDrafts: false });
---

<BlogArchiveList posts={posts} />
