---
import Footer from "./Footer.astro";

interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<div class={`flex flex-col w-full bg-ivory transition-colors dark:bg-zinc-950 ${className}`}>
  <main class="flex-1 w-full px-2 md:px-0">
    <slot />
  </main>
  <Footer />
</div>
