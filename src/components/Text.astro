---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<p class={`max-w-[75ch] leading-relaxed text-zinc-700 dark:text-zinc-300 ${className}`}>
  <slot />
</p>
