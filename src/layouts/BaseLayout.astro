---
import "@fontsource/geist/400.css";
import "@fontsource/geist/500.css";
import "@fontsource/geist/600.css";
import "@fontsource/geist/700.css";
import "@fontsource/geist-mono/400.css";
import "@fontsource/geist-mono/500.css";
import "@fontsource/geist-mono/600.css";
import "@fontsource/geist-mono/700.css";
import "../styles/tailwind.css";
import Nav from "../components/Nav.astro";
import { getOgImageUrl } from "../lib/og";
import { ClientRouter } from "astro:transitions";

type Props = {
  title?: string;
  description?: string;
  og?: string;
  type?: string;
  noindex?: boolean;
  structuredData?: Record<string, unknown> | Record<string, unknown>[];
};

const {
  title = "Nick Radford",
  description = "Staff Product Engineer in San Francisco building full-stack SaaS, internal tools, design systems, and developer platforms.",
  og = undefined,
  type = "website",
  noindex = false,
  structuredData = undefined,
} = Astro.props;

const canonicalUrl = new URL(Astro.url.pathname, Astro.site ?? Astro.url.origin).toString();
const structuredDataItems = structuredData
  ? Array.isArray(structuredData)
    ? structuredData
    : [structuredData]
  : [];
const serializeStructuredData = (data: Record<string, unknown>) =>
  JSON.stringify(data).replace(/</g, "\\u003c");
const socialImagePath = getOgImageUrl(og ?? "/og/nick-radford-dot-dev.png");
const socialImageUrl = new URL(
  socialImagePath,
  Astro.url.origin,
).toString();
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, viewport-fit=cover"
    />
    <meta name="theme-color" content="#09090b" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    <script is:inline>
      if (window.location.hash) {
        document.documentElement.dataset.initialHashScroll = "true";
      }
    </script>
    <style is:inline>
      html[data-initial-hash-scroll="true"] body {
        visibility: hidden;
      }
    </style>
    <!-- other meta tags -->
    <meta property="og:image" content={socialImageUrl} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <meta property="og:image:type" content="image/png" />
    <meta property="og:image:alt" content={title} />
    <meta property="og:type" content={type} />
    <meta property="og:url" content={canonicalUrl} />

    <!-- og title and description -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={socialImageUrl} />

    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <link rel="icon" href="/favicon.ico" sizes="any" />
    <link
      rel="stylesheet"
      type="text/css"
      href="https://cdn.jsdelivr.net/npm/@phosphor-icons/web@2.1.2/src/regular/style.css"
    />
    <link
      rel="alternate"
      type="application/rss+xml"
      title="Nick Radford (dot) Dev"
      href={new URL("feeds/atom.xml", Astro.site)}
    />
    <title>{title}</title>
    <meta name="description" content={description} />
    {noindex && <meta name="robots" content="noindex,follow" />}
    <link rel="canonical" href={canonicalUrl} />
    <link rel="author" href="https://nickradford.dev" />
    {
      structuredDataItems.map((item) => (
        <script
          is:inline
          type="application/ld+json"
          set:html={serializeStructuredData(item)}
        />
      ))
    }
    <script
      is:inline
      src="https://analytics.ahrefs.com/analytics.js"
      data-key="3J+Xlk9VXb7hR1h9xaoxeQ"
      async></script>
    <script is:inline>
      (() => {
        const storageKey = "theme-toggle";
        const themeColors = {
          dark: "#09090b",
          light: "#fffff0",
        };
        let currentTheme;

        function getStoredTheme() {
          const theme = localStorage.getItem(storageKey);
          return theme === "dark" || theme === "light" ? theme : null;
        }

        function getTheme() {
          return currentTheme || getStoredTheme() || "dark";
        }

        function setStoredTheme(theme) {
          if (theme === "dark") {
            localStorage.removeItem(storageKey);
          } else {
            localStorage.setItem(storageKey, theme);
          }
        }

        function setThemeColor(theme) {
          let themeColorMeta = document.querySelector('meta[name="theme-color"]');

          if (!themeColorMeta) {
            themeColorMeta = document.createElement("meta");
            themeColorMeta.setAttribute("name", "theme-color");
            document.head.appendChild(themeColorMeta);
          }

          themeColorMeta.setAttribute("content", themeColors[theme]);

          const appleStatusBarMeta = document.querySelector(
            'meta[name="apple-mobile-web-app-status-bar-style"]',
          );
          appleStatusBarMeta?.setAttribute(
            "content",
            theme === "dark" ? "black-translucent" : "default",
          );
        }

        function setStyle(theme) {
          const root = document.documentElement;
          root.classList.toggle("dark", theme === "dark");
          root.dataset.theme = theme;
          root.style.colorScheme = theme;
          root.style.backgroundColor = themeColors[theme];
          setThemeColor(theme);
        }

        function setTheme(theme) {
          currentTheme = theme;
          setStoredTheme(theme);
          setStyle(theme);
        }

        function setupTheme() {
          setTheme(getTheme());
        }

        setupTheme();
        document.addEventListener("astro:after-swap", setupTheme);
        window.astroThemeToggle = { setTheme, getTheme };
      })();
    </script>
    <ClientRouter />
  </head>
  <body
    class="transition-colors bg-ivory text-zinc-900 dark:bg-zinc-950 dark:text-zinc-50 min-h-screen"
  >
    <div class="flex flex-col items-center w-full min-h-fill-available flex-1">
      <Nav transition:persist />
      <slot />
      <script is:inline>
        (function () {
          if (!document.documentElement.dataset.initialHashScroll) {
            return;
          }

          var targetId = decodeURIComponent(window.location.hash.slice(1));
          var target = document.getElementById(targetId);

          if (!target) {
            delete document.documentElement.dataset.initialHashScroll;
            return;
          }

          function scrollToTarget() {
            var scrollPaddingTop =
              parseFloat(
                window.getComputedStyle(document.documentElement).scrollPaddingTop,
              ) || 0;
            var top =
              target.getBoundingClientRect().top + window.scrollY - scrollPaddingTop;

            window.scrollTo(0, top);
          }

          scrollToTarget();
          window.requestAnimationFrame(function () {
            scrollToTarget();
            window.requestAnimationFrame(function () {
              delete document.documentElement.dataset.initialHashScroll;
            });
          });
        })();
      </script>
      <script
        defer
        is:inline
        src="https://st.nickradford.dev/script.js"
        data-website-id="310a6df8-1977-4cdc-9650-6fc42b11dd23"
        data-domains="nickradford.dev"></script>
      <script>
        function updateActiveLinks() {
          const currentPath = window.location.pathname;

          const isActive = (href: string): boolean => {
            if (href === "/") {
              return currentPath === "/";
            }
            return currentPath.startsWith(href);
          };

          const navLinks = [
            { label: "home", href: "/" },
            { label: "writing", href: "/blog" },
            { label: "work", href: "/work" },
            { label: "links", href: "/links" },
          ];

          // Update desktop nav
          const pageHeaderNav = document.querySelector("#page-header-nav");
          if (pageHeaderNav) {
            navLinks.forEach((navLink) => {
              const links = Array.from(
                pageHeaderNav.querySelectorAll(
                  `a[href="${navLink.href}"]:not([data-not-active-styled])`,
                ),
              );
              const active = isActive(navLink.href);

              links.forEach((link) => {
                if (active) {
                  link.classList.add(
                    "text-orange",
                    "dark:text-orange",
                    "md:bg-orange/5",
                  );
                } else {
                  link.classList.remove(
                    "text-orange",
                    "dark:text-orange",
                    "md:bg-orange/5",
                  );
                }
              });
            });
          }

          // Update mobile nav
          const pageHeaderNavMobile = document.querySelector(
            "#page-header-nav-mobile",
          );
          if (pageHeaderNavMobile) {
            navLinks.forEach((navLink) => {
              const links = Array.from(
                pageHeaderNavMobile.querySelectorAll(
                  `a[href="${navLink.href}"]:not([data-not-active-styled])`,
                ),
              );
              const active = isActive(navLink.href);

              links.forEach((link) => {
                if (active) {
                  link.classList.add("text-orange", "dark:text-orange");
                } else {
                  link.classList.remove("text-orange", "dark:text-orange");
                }

                // Handle mobile underline
                const span = link.querySelector("span");
                if (active && !span) {
                  const newSpan = document.createElement("span");
                  newSpan.className =
                    "md:hidden absolute bottom-0 left-0 right-0 h-[2px] bg-orange";
                  link.appendChild(newSpan);
                } else if (!active && span) {
                  span.remove();
                }
              });
            });
          }
        }

        function enableSmoothScrollAfterInitialPosition() {
          delete document.documentElement.dataset.smoothScroll;

          const enableSmoothScroll = () => {
            document.documentElement.dataset.smoothScroll = "true";
          };

          const revealPage = () => {
            delete document.documentElement.dataset.initialHashScroll;
          };

          if (!window.location.hash) {
            revealPage();
            enableSmoothScroll();
            return;
          }

          const scrollToHashTarget = () => {
            const targetId = decodeURIComponent(window.location.hash.slice(1));
            const target = document.getElementById(targetId);

            if (!target) {
              return false;
            }

            const scrollPaddingTop =
              Number.parseFloat(
                window.getComputedStyle(document.documentElement).scrollPaddingTop,
              ) || 0;
            const top =
              target.getBoundingClientRect().top + window.scrollY - scrollPaddingTop;

            window.scrollTo({ top, behavior: "auto" });
            return true;
          };

          const hashTargetFound = scrollToHashTarget();

          if (!hashTargetFound) {
            revealPage();
            enableSmoothScroll();
            return;
          }

          window.requestAnimationFrame(() => {
            scrollToHashTarget();
            window.requestAnimationFrame(() => {
              revealPage();
              enableSmoothScroll();
            });
          });
        }

        // Initial update on page load
        updateActiveLinks();
        enableSmoothScrollAfterInitialPosition();

        // Update on navigation
        document.addEventListener("astro:page-load", updateActiveLinks);
        document.addEventListener(
          "astro:page-load",
          enableSmoothScrollAfterInitialPosition,
        );
        document.addEventListener("astro:before-preparation", () => {
          delete document.documentElement.dataset.smoothScroll;
        });
      </script>
    </div>
  </body>
</html>
